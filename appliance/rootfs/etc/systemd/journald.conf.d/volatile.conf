# Read-only root: keep logs in RAM, never written to the SD. Runtime logs come from
# `journalctl` (this boot) and `docker logs`; the constitution does not persist them.
[Journal]
Storage=volatile
RuntimeMaxUse=32M
